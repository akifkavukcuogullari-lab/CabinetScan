# Server Pipeline V2 — Python Dependencies
# GPU pipeline for cabinet measurement from video

# Core ML
torch>=2.7.0
torchvision>=0.22.0

# Depth Estimation
# Video Depth Anything (installed from source or HuggingFace)
transformers>=4.36.0
accelerate>=0.25.0

# Segmentation
# SAM 3 (Meta) — text-prompted instance segmentation
sam3 @ git+https://github.com/facebookresearch/sam3.git
# SAM 3 transitive deps (training/data code imported at module level)
einops>=0.7.0
decord>=0.6.0
hydra-core>=1.3.0
pycocotools>=2.0.0
scikit-learn>=1.3.0
fvcore>=0.1.5
fairscale>=0.4.0
torchmetrics>=1.0.0
zstandard>=0.21.0

# Computer Vision
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0
scikit-image>=0.21.0
Pillow>=10.0.0

# 3D / Geometry
open3d>=0.17.0

# API / Infrastructure
runpod>=1.6.0
supabase>=2.0.0
httpx>=0.25.0
python-dotenv>=1.0.0

# Floor Plan Generation
matplotlib>=3.7.0
reportlab>=4.0.0

# VLM Validation
openai>=1.6.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
